version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    # for local development you might want to mount code as volumes
    # volumes:
    #   - ./server:/app/server
    #   - ./fitness-client:/app/fitness-client
